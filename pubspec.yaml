name: ainotes
description: "Voice note organizer — record, transcribe, rewrite, classify, file."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0-130.0.dev

dependencies:
  flutter:
    sdk: flutter

  # State management + DI
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Navigation
  go_router: ^14.8.1

  # Data models
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Storage
  objectbox: ^4.0.3
  objectbox_flutter_libs: ^4.0.3
  path_provider: ^2.1.5
  yaml: ^3.1.3

  # UI
  flutter_animate: ^4.5.2
  flutter_staggered_grid_view: ^0.7.0

  # Audio Recording (Phase 2)
  record: ^6.2.0
  permission_handler: ^12.0.1

  # On-device STT (Phase 2)
  sherpa_onnx: ^1.12.24

  # Audio visualization (Phase 2)
  audio_waveforms: ^1.1.1

  # Preferences
  shared_preferences: ^2.3.4

  # Utilities
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  freezed: ^2.5.8
  json_serializable: ^6.9.4
  objectbox_generator: ^4.0.3
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
